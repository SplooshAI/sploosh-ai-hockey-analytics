# Enforce engine requirements
engine-strict=true

# Save exact versions (no ^ or ~)
save-exact=true

# Disable package-lock.json updates during install
package-lock=true

# Audit level
audit-level=moderate
